spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://[your db Address]:3306/wktmdb?useSSL=false&characterEncoding=UTF-8
    username: [username]
    password: [password]

mybatis:
 mapper-locations: classpath*:/mapper/*.xml
